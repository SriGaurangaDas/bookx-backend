package com.bookx.bookx_backend.model.enums;

public enum BookCondition {
    NEW,        // Brand new, unread
    LIKE_NEW,   // Read once, no visible wear
    VERY_GOOD,  // Minimal wear, clean pages
    GOOD,       // Some wear, but still good shape
    FAIR,       // Noticeable wear, all pages intact
    ACCEPTABLE  // Heavy wear, but readable
}